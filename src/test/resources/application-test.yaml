spring:
  config:
    activate:
      on-profile: ci, test

  datasource:
    url: jdbc:h2:mem:tasklist
    username: test
    password: test
    driver-class-name: org.h2.Driver

  liquibase:
    enabled: false
